{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/Backend/attached_assets/package.json",
                "localRoot": "${workspaceFolder}/Backend/attached_assets"
            }
        }
    ]
}